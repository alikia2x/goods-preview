# Copy to `.env.local` for a local build that reports to GA4. Analytics only
# ever runs in a production build (`vite build`), never on a dev server.
VITE_ANALYTICS_ENABLED=true
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
